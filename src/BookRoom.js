import Client from "./Contentful";
  

export default function bookRoom(roomId) {
    alert ("hello")

    alert(roomId);

    // let response = Client.getEntry(roomId).then(entry => {
    //     console.log(entry);
    // })

    // console.log(response)

    // client.getEntry(roomId)
    // .then((entry) => console.log(entry))
    // .catch(console.error)
}